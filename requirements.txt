# Python >= 3.12 (lerobot). CPU-only torch AND torchvision: install both first
# from the CPU index, or lerobot pulls the CUDA torchvision build from PyPI:
#   pip install torch==2.11.0 torchvision==0.26.0 --index-url https://download.pytorch.org/whl/cpu
# Versions are the ones every number in the README was measured on.
so-arm100-sim[lerobot] @ git+https://github.com/AungKaung1928/so-arm100-sim.git@v0.1.0
torch==2.11.0
torchvision==0.26.0
numpy==2.2.6
lerobot[smolvla,dataset]==0.6.1
sentence-transformers==6.1.0
transformers==5.5.4
mujoco==3.13.0
pytest
